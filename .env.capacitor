# Used by `vite build --mode capacitor` (mobile shell branch).
# API calls from the native WebView are rewritten to https://nexcourse.ai
# via src/lib/nativeApiBridge.ts unless VITE_API_BASE is set.
CAPACITOR=1
