# Used by `vite build --mode capacitor` (mobile shell branch).
# Override local-dev VITE_API_BASE from `.env` (often http://localhost:3001) —
# the Android emulator has no Express server on localhost.
CAPACITOR=1
VITE_API_BASE=https://nexcourse.ai
