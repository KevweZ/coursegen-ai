# Cloudflare Workers — Routing Rules

# 1. Proxy /api/* calls to the Railway backend
/api/*  https://coursegen-ai-production.up.railway.app/api/:splat  200

# NOTE: SPA fallback is handled by wrangler.jsonc (not_found_handling: single-page-application)
# Do NOT add a /* rule here — it causes an infinite loop with wrangler's SPA mode
