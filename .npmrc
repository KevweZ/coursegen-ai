# Ensure native addons don't break the Cloudflare Pages frontend build.
# better-sqlite3 and similar C++ addons are server-only (Railway).
# Cloudflare Pages only needs the Vite/React frontend dependencies.
ignore-scripts=false
optional=true
