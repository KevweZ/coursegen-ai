Neutral quiz chrome, module-matched bullets, quieter bold, and fix Begin Quiz

- Slide bullets use the module accent (or near-black) instead of hardcoded indigo.
- Strip partial **bold** from bullet lines at render time and soften remaining
  strong tags so lists don't compete with bold titles; AI prompt updated too.
- Mastery Quiz intro/questions and the player Next button move from indigo to
  dark slate / gray to match the white theme.
- Begin Mastery Quiz: resolve the quiz slide by id, show spinner + error text,
  and refuse to navigate when generation fails (was failing silently).
